#include <iostream>
#include <string>

using namespace std;

int main() {
    while (true) {
        string command;
        getline(cin, command);
        cerr << "unknown command" << endl;
    }
    return 0;
}
